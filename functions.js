// FUNCTIONS.JS - Funções auxiliares para o projeto Energia Sustentável

// Função básica para debug
function debugLog(message) {
    if (console && console.log) {
        console.log('🔧 Functions.js:', message);
    }
}

// Verificar se está carregado
debugLog('Functions.js carregado com sucesso');

// Funções utilitárias podem ser adicionadas aqui conforme necessário
